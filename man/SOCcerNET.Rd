% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soccer.R
\name{SOCcerNET}
\alias{SOCcerNET}
\alias{run_soccernet}
\alias{soccernet}
\alias{CLIPS}
\alias{clips}
\alias{run_clips}
\title{Occupation or Industry coding}
\usage{
run_soccernet(df, n = 10, block_size = 8)

run_clips(df, n = 10, block_size = 20)
}
\arguments{
\item{df}{A data frame containing the text to classify. See Details for
required columns.}

\item{n}{Number of candidate codes to return per row (default: \code{10})}

\item{block_size}{Number of rows to process per batch, tune for performance
(default: \code{8} for SOCcerNET, \code{20} for CLIPS)}
}
\value{
The input data frame with additional columns containing the top \code{n}
classification codes and their scores, joined by \code{Id}.
}
\description{
Classifies job descriptions or product/service descriptions into occupational
or industry codes. \code{run_soccernet()} codes jobs into US SOC 2010 codes.
\code{run_clips()} codes products and services into industry
classifications. SOCcerNET can use optional soc1980, noc2011, or isco1988
columns to assist in the soc2010 coding.  CLIPS can use a sic1987 columns.
}
\details{
\subsection{Required columns}{

\strong{\code{run_soccernet()}} requires:
\itemize{
\item \code{JobTitle} — job title text \emph{(required)}
\item \code{JobTask} — job task description \emph{(optional, created as empty string if missing)}
\item \code{Id} — unique row identifier \emph{(optional, auto-generated as \code{row-001}, \code{row-002}, etc. if missing)}
}

Optionally, crosswalk columns \code{soc1980}, \code{noc2011}, and \code{isco1988} can be
provided as lists or character vectors.

\strong{\code{run_clips()}} requires:
\itemize{
\item \code{products_services} — product or service description \emph{(required)}
\item \code{Id} — unique row identifier \emph{(optional, auto-generated if missing)}
}
}

\subsection{Performance}{

\code{block_size} controls how many rows are processed at once. Larger values
may improve throughput but use more memory. Tune based on your hardware.
}
}
\examples{
\dontrun{
# SOCcerNET
df <- data.frame(
  JobTitle = c("Staff Scientist", "Data Engineer"),
  JobTask  = c("Develop chemical assays", "Build data pipelines")
)
run_soccernet(df)
run_soccernet(df, n = 5, block_size = 16)

# CLIPS
df <- data.frame(products_services = c("Software development", "Cloud hosting"))
run_clips(df)
}

}
\seealso{
\code{\link[=embed_jobs]{embed_jobs()}}
}
